# PaperJoy · 更新说明（v2.0.4）

> 学术文献深刻剖析系统（展示名 **PaperJoy**，中文名「**文献悦读**」）
> 发布日期：2026-08-17
> 版本号：2.0.4（相对 2.0.3 的增量更新）

---

## 一句话概述

本次更新让 PaperJoy 在 **PDF 输入**时具备「**图表自动裁剪 + 一键内嵌**」能力：附带一个仅依赖纯 Python 包 `PyMuPDF` 的轻量工具 `tools/extract_figures.py`，**首次用户只需 `pip install pymupdf`（一次），之后每次分析 PDF 即可全自动把原文全部 Figures / Tables 真图嵌入生成的 Markdown**，无需安装任何系统级依赖（如 poppler），跨平台可离线使用。

---

## ✨ 本次更新亮点（v2.0.4）

### 1. 新增「图表自动裁剪工具」（PDF 输入核心能力）
- 新增随技能附带的工具 **`tools/extract_figures.py`**：
  - 只依赖一个纯 Python 包 **PyMuPDF**，**无需 poppler**、**跨平台（Windows / macOS / Linux）**、**可离线安装**。
  - 自动抽取 PDF 中**全部** Figures 与 Tables，保存为 `fig<N>.png` / `tbl<N>.png`（命名对齐原文 Figure / Table 编号）。
  - 输出「编号 → 文件」清单，并写出机器可读的 `manifest.json`（含是否需人工校对标注）。
- **首次使用零系统依赖**：新用户不必折腾 poppler / ImageMagick，装好 PyMuPDF 即功能完整。

### 2. 首次安装指引（仅一次）
- SKILL.md 顶部新增 **「〇、首次使用前：安装图表自动裁剪工具（仅需一次）」** 段落：
  ```
  pip install pymupdf
  ```
- 装好后，每次 PDF 输入按模块一 1.5 运行工具即可，全程无障碍。

### 3. 模块一 1.5 重写为「双路径」
- **默认主路径（推荐）**：PyMuPDF 一键自动裁剪，首次使用零系统依赖、安装后即全自动。
- **回退路径**：已装 `poppler` 的用户仍可走手动 `pdftoppm` / `pdfimages` 流程（图/表均显式重命名为 `fig<N>.png` / `tbl<N>.png`，与内嵌命名严格对齐）。
- 矢量图 / 复杂排版会触发「启发式裁剪」并**明确标注需人工校对**，绝不编造图表。

### 4. 文档与发布元数据同步
- README 新增 **「🖼️ 图表自动裁剪工具」** 说明段 + 目录结构补充 `tools/extract_figures.py`。
- `manifest.yaml` 升至 **2.0.4**，changelog 同步。
- `PROMPT.md` 由 SKILL.md 重新生成，已含全部新约束（按 `.gitignore` 约定不进版本跟踪，走 Release 资产）。

---

## 📋 完整变更清单

### v2.0.4（2026-08-17）
- 新增「图表自动裁剪工具」：PDF 输入时一键内嵌原文全部 Figures / Tables。
  - 新增 `tools/extract_figures.py`（仅依赖 PyMuPDF，跨平台、可离线）。
  - SKILL.md 新增「〇、首次使用前」安装段（`pip install pymupdf`，仅需一次）。
  - 模块一 1.5 重写为「PyMuPDF 一键裁剪（默认）+ poppler 手动回退」双路径。
  - README 新增说明段并更新目录结构；manifest 升至 2.0.4。

### v2.0.3（2026-08-17）
- 修复 **Mermaid 时间线底部文字重叠**：
  - 模块五 ② 时间线图新增「防重叠硬约束」——节点数 ≤ 9、每节点固定 `时间 : 阶段短语(≤8字) : 极简动作(≤12字)`、禁止整句描述与 `；` 并列。
  - 时间线仅做时间轴总览，完整细节移至③试验时间分配表。
  - 示例改为极简短标签版，根治节点一多 / 文字一长就重叠的渲染缺陷。

*(更早版本见 README「🔄 更新日志」：2.0.2 命名文案规范化、2.0.1 Tables 默认呈现翻转为原表截图、2.0.0 原文图表抽取与内嵌基础能力。)*

---

## 🔧 安装 / 升级指引

### 全新安装
1. 将本仓库 `paper-joy/` 整个文件夹，按目标客户端要求放入 skills 目录（如 WorkBuddy：`~/.workbuddy/skills/paper-joy/`）。
2. **启用图表自动裁剪（仅需一次）**：
   ```
   pip install pymupdf
   ```
3. （可选）重新生成 `PROMPT.md`：
   ```
   python tools/build_prompt.py
   ```

### 从旧版本升级
- 直接覆盖 `paper-joy/` 目录即可（`PROMPT.md` 为自动生成，覆盖后会自动/手动重建；`.gitignore` 已排除生成物）。
- **首次使用前补装一次** `pip install pymupdf`，即可激活 v2.0.4 的图表自动裁剪能力。

---

## 📌 使用要点 & 已知限制

- **何时触发截图内嵌**：输入为 **PDF 文件 / 路径**时自动抽取内嵌；纯文本 / 摘要输入则跳过（按设计）。
- **未装 PyMuPDF 时**：降级为文字引用 + 图题概述，**绝不编造图表**，并在图文映射处标注未生成原因。
- **矢量图 / 复杂排版**：工具采用启发式裁剪，可能需人工微调边界（清单与 `manifest.json` 会标注「需校对」）。
- **命名约定**：原文 Figure 1 → `fig1.png`，Table 2 → `tbl2.png`；跨页表自动竖向拼接为单张。

---

## 📂 本版文件清单

```
paper-joy/
├── SKILL.md                  # ★ 唯一手工维护文件（含 v2.0.4 全部指令 + 首次安装段）
├── tools/
│   ├── build_prompt.py       # 由 SKILL.md 生成 PROMPT.md（单一信息源构建器）
│   └── extract_figures.py    # 🆕 图表自动裁剪工具（仅依赖 PyMuPDF）
├── manifest.yaml             # 市场发布元数据（name=paper-joy, version=2.0.4）
├── README.md                 # 含「🖼️ 图表自动裁剪工具」说明 + 更新日志
├── LICENSE                   # MIT
├── PROMPT.md                 # （自动生成，未跟踪）通用提示词版
└── .gitignore                # 排除生成物（PROMPT.md / 分发 zip）
```
